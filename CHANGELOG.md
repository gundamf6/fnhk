# 更新日志

## 0.4.0
- 新增 Docker 部署支持（可在群晖/威联通/极空间/绿联/树莓派/任意 Linux 上运行；镜像自带 ffmpeg）
- 新增 `/healthz` 健康检查接口
- 支持环境变量配置：`NVR_HTTP_USER` / `NVR_HTTP_PASS` / `NVR_PORT` / `NVR_REC_ROOT` / `NVR_RETENTION_DAYS` / `NVR_SEGMENT_SECONDS` 等
- Docker 首次启动使用默认口令 `admin` / `admin`，容器日志会打印访问地址与口令；登录后可在「设置 → 🔑 访问口令」自助修改；忘记口令可用 `-e NVR_HTTP_PASS=新口令` 重置
- 启动时打印真实访问地址：Docker 下自动探测本机局域网 IP；探测不到时用第一次浏览器访问的 Host 补打
- 修复（重要）：Docker 下「清理孤儿拉流进程」的巡检会把正在录像的拉流进程误判成孤儿，**每 60 秒杀掉一次**（表现为录像每分钟断几秒）。原因：容器里本程序就是 1 号进程，自己拉起的子进程父 PID 同样是 1；改为按本进程已拉起的 pid 白名单判定
- 修复：设置页「选择保存目录」弹窗顶部新增「存储空间（硬盘）」列表，可在任意层级一键切换到其它硬盘（此前只能选到默认那块盘）；Docker 下只列出容器里映射的录像目录
- 修复：深色主题下品牌下拉框选项白底白字看不清（补 `color-scheme: dark`）
- 改进：跨小时 / 跨天切换录像目录时不再等 5 秒退避，立即恢复拉流（切换空档从 5 秒降到 0.2 秒）
- 改进：测试连接失败时返回 ffmpeg 原始错误（诊断详情），便于远程定位「连不上」的原因

## 0.3.0
- 多品牌支持：海康 / 大华 / 宇视 / TP-LINK 模板 + 「自定义 RTSP」
- ONVIF 自动发现（WS-Discovery）与自动取流
- 允许删除全部摄像机；重复添加检测；认证失败长退避；安装依赖自动化

## 0.2.2
- 多摄像机（最多 4 台），各机独立管线、独立录像目录
- 首页改为「摄像机快照列表」，点入单画面（同一时间只播一路）
- 回放按 天 / 上午·下午 / 小时 三级折叠；片段带摄像机名前缀
- 直播与回放均支持全屏；竖屏全屏时按钮按「横屏后看到的方位」摆放（右上角）
- 录像目录可在设置页点选；目录浏览/新建目录
- 稳定性：HTTP 层防双写头、进程级异常兜底（录像不因单个坏请求中断）、损坏尾段自动清理

## 0.2.1
- 首页改为快照入口 + 单画面页；声音/抓拍/全屏按钮移到画面下方
- 快照只在「进软件 / 回到前台 / 从直播页返回」时抓取，不做定时轮询

## 0.2.0
- 多摄像机架构（`cameras[]`）、每台 2 路会话（主码流 + 子码流）
- 新增目录浏览 API、移动侦测各机独立计算

## 0.1.0
- 首个版本：实时预览、连续 + 事件双轨录像、网页回放、本地移动侦测、设置页配置（IP/账号/通道/码流/目录）
